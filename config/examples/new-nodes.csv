# Spec file for scripts/add-nodes.sh (day-2: adding nodes to an
# already-installed cluster). Same schema as nodes.csv plus one extra
# trailing column - copy this file to config/new-nodes.csv and list ONLY the
# new machines you want to add.
#
# Only role=worker and role=storage are supported for day-2 additions.
#
# role=storage is a plain worker as far as OpenShift is concerned. What the
# role changes is the hardware: the node gets the extra data disks listed in
# storage_disks_gb, attached to a VirtualNVMEController (ODF/LSO expects NVMe
# devices), while the OS disk stays on pvscsi like every other node. Once the
# node joins, add-nodes.sh labels it cluster.ocs.openshift.io/openshift-storage
# so ODF will actually consume it.
#
#   storage_disks_gb   one or more data disk sizes in GB, separated by "|",
#                      e.g. 500 for a single disk, 500|500|500 for three.
#                      Leave EMPTY for role=worker - it is rejected there.
#
# Resulting layout for a storage node (govc device.info):
#   nvme-31000    VirtualNVMEController   "NVME controller 0"
#   disk-31000-0  VirtualDisk             "Hard disk 2"   <- first data disk
name,role,mac_mode,mac,ip,prefix,gateway,dns,cpu,memory_mb,disk_gb,storage_disks_gb
worker-2,worker,dynamic,,192.0.2.15,24,192.0.2.1,192.0.2.2|192.0.2.3,8,16384,120,
storage-0,storage,dynamic,,192.0.2.16,24,192.0.2.1,192.0.2.2|192.0.2.3,16,32768,120,500|500
